# Homebrew Tap for Tracelines

Official Homebrew formula tap for **Tracelines** (Traceability-as-code CLI).

## Installation

```bash
# 1. Tap the repository
brew tap f39d/tracelines

# 2. Install the CLI
brew install tracelines
```
